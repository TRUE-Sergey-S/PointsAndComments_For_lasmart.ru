function Draw() {
}

    /*
    var width = window.innerWidth;
            var height = window.innerHeight;

            var stage = new Konva.Stage({
                container: 'container',
                width: width,
                height: height
            });

            var layer1 = new Konva.Layer();

    var circle = new Konva.Circle({
        x: positionX, // задаем координату x
        y: positionY, // и координату y
        radius: radius,// радиус окружности
        fill: color, // цвет заливки
    });
            /*
            var rect = new Konva.Rect({
                x: 55, // задаем координату x
                y: 55, // и координату y
                width: 90, // ширина
                height: 90, // высота
                fill: 'red', // цвет заливки
                stroke: 'black', // цвет обводки
                strokeWidth: 4 // ширина обводки
            });

            // добавить фигуры на слой
            layer1.add(rect);
            layer1.add(circle);
            

            // добавить слой на лист
    stage.add(layer1);
    */
